-- plugin/pairy.lua
-- Auto-sourced by Neovim when pairy is on the runtimepath.
-- The actual setup is triggered by the LazyVim plugin spec (config = function()).
-- This file just prevents double-sourcing.

if vim.g.pairy_loaded then return end
vim.g.pairy_loaded = true
