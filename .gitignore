# Never commit secrets or local config
*.env
.env*
config.json
*_secret*
*_key*

# Claude Code workspace settings (developer-local, not project config)
# Use .claude/* (not .claude/) so the !skills exception can work
.claude/*
!.claude/skills/
!.claude/skills/**

# Neovim runtime logs
.nvimlog
